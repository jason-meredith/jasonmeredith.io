import { ArticleLayout } from '@/components/ArticleLayout'

export const article = {
  author: 'Jason Meredith',
  date: '2024-11-21',
  title: 'How to Hack a Satellite',
  description:
    'Deep dive into satellite radio frequencies and how to pick up and decode them'
}

export const metadata = {
  title: article.title,
  description: article.description,
}

export default (props) => <ArticleLayout article={article} {...props} />

(possibly clickbait)

Intertwine an essay of why Goldeneye is so great with how to decode NOAA satellite imagery